export const metadata = {
  title: 'Request data deletion | Globalbanka',
  description: 'Submit a request to delete your personal data from the Globalbanka app.',
};

export default function DataDeletionLayout({ children }) {
  return children;
}
