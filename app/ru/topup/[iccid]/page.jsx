'use client';

import React from 'react';
import TopupPage from '../../../../topup/[iccid]/page';

// Russian locale version uses the same component
export default function RussianTopupPage() {
  return <TopupPage />;
}